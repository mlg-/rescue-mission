<%= form_for @answer do |f| %>
  <%= f.label :description, 'Your Answer' %>
  <%= f.text_field :description %>
  <%= hidden_field_tag(:question_id, "#{@question.id}") %>
  <%= f.submit 'Save the day!' %>
<% end %>
