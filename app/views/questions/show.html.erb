<h1><%= @question.title %></h1>

<p><%= @question.body %></p>
<%= link_to 'Edit this question', edit_question_path(id: @question.id) %>

<h2>Answer this Question</h2>

<%= render "answers/new" %>

<h2>Other Answers</h2>
<ul>
  <% unless @answers.nil? %>
    <% @answers.each do |answer| %>
      <li><%= answer.description %></li>
    <% end %>
  <% end %>
</ul>
